{
  "name": "@stnew/prismic-react",
  "version": "1.2.3",
  "description": "Prismic components and utilities for React",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "author": "Something New",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "react": ">= 16.8.1",
    "react-dom": ">= 16.8.1"
  },
  "dependencies": {
    "@stnew/prismic": "^1.2.3",
    "@stnew/prismic-types": "^1.2.3",
    "prismic-reactjs": "^1.3.2"
  }
}

{
  "name": "@stnew/prismic",
  "version": "1.0.0",
  "description": "Prismic components and utilities for React",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "author": "Something New",
  "license": "MIT",
  "peerDependencies": {
    "react": "^16.8.1",
    "react-dom": "^16.8.1"
  },
  "dependencies": {
    "@stnew/prismic-types": "^1.0.0"
  },
  "devDependencies": {
    "@types/react": "^16.9.36",
    "@types/react-dom": "^16.9.8",
    "typescript": "^3.9.5"
  }
}

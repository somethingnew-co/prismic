{
  "name": "@stnew/prismic",
  "version": "1.0.1",
  "description": "Prismic components and utilities for React",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "author": "Something New",
  "license": "MIT",
  "dependencies": {
    "@stnew/prismic-types": "^1.0.1"
  }
}
